# i18n (EN + UA) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add English + Ukrainian i18n to keyo.studio using next-intl with URL-prefix routing (`/en/…`, `/ua/…`), browser-language auto-detection, and an EN|UA toggle pill in the Navbar.

**Architecture:** All pages move from `app/` into `app/[locale]/`. Middleware chains next-intl locale detection with Clerk auth. Locale-aware `Link`/`useRouter`/`usePathname` helpers are exported from `i18n/navigation.ts` and replace all `next/navigation`+`next/link` imports that touch internal routes.

**Tech Stack:** Next.js 14.2, next-intl v3, Clerk v7, TypeScript, Tailwind

---

## File Map

| Action | Path |
|--------|------|
| Install | `next-intl` |
| Create | `i18n/routing.ts` |
| Create | `i18n/request.ts` |
| Create | `i18n/navigation.ts` |
| Modify | `next.config.js` |
| Create | `messages/en.json` |
| Create | `messages/ua.json` |
| Modify | `middleware.ts` |
| Modify | `app/layout.tsx` (strip to shell) |
| Create | `app/[locale]/layout.tsx` |
| Move+edit | `app/page.tsx` → `app/[locale]/page.tsx` |
| Move+edit | `app/pricing/page.tsx` → `app/[locale]/pricing/page.tsx` |
| Move | `app/privacy/page.tsx` → `app/[locale]/privacy/page.tsx` |
| Move | `app/terms/page.tsx` → `app/[locale]/terms/page.tsx` |
| Move | `app/dashboard/**` → `app/[locale]/dashboard/**` |
| Move | `app/settings/**` → `app/[locale]/settings/**` |
| Move | `app/image/**` → `app/[locale]/image/**` |
| Move | `app/video/**` → `app/[locale]/video/**` |
| Move | `app/audio/**` → `app/[locale]/audio/**` |
| Move | `app/editor/**` → `app/[locale]/editor/**` |
| Move+edit | `app/sign-in/**` → `app/[locale]/sign-in/**` |
| Move+edit | `app/sign-up/**` → `app/[locale]/sign-up/**` |
| Move+edit | `app/sso-callback/**` → `app/[locale]/sso-callback/**` |
| Create | `components/layout/LocaleSwitcher.tsx` |
| Modify | `components/layout/Navbar.tsx` |
| Modify | `components/layout/Footer.tsx` |
| Modify | `components/home/CommunityGallery.tsx` |
| Modify | `app/sitemap.ts` |

---

## Task 1: Install next-intl

**Files:** none (package.json)

- [ ] **Step 1: Install**

```bash
npm install next-intl
```

Expected: `next-intl` appears in `package.json` dependencies, no peer-dependency errors.

- [ ] **Step 2: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: install next-intl"
```

---

## Task 2: Create i18n config files

**Files:**
- Create: `i18n/routing.ts`
- Create: `i18n/request.ts`
- Create: `i18n/navigation.ts`

- [ ] **Step 1: Create `i18n/routing.ts`**

```ts
// i18n/routing.ts
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['en', 'ua'] as const,
  defaultLocale: 'en',
  localePrefix: 'always',
});
```

- [ ] **Step 2: Create `i18n/request.ts`**

```ts
// i18n/request.ts
import { getRequestConfig } from 'next-intl/server';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;
  if (!locale || !(routing.locales as readonly string[]).includes(locale)) {
    locale = routing.defaultLocale;
  }
  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 3: Create `i18n/navigation.ts`**

```ts
// i18n/navigation.ts
import { createNavigation } from 'next-intl/navigation';
import { routing } from './routing';

export const { Link, redirect, usePathname, useRouter } = createNavigation(routing);
```

- [ ] **Step 4: Commit**

```bash
git add i18n/
git commit -m "feat(i18n): add next-intl routing, request, navigation config"
```

---

## Task 3: Update next.config.js

**Files:**
- Modify: `next.config.js`

- [ ] **Step 1: Add withNextIntl plugin**

Replace the top of `next.config.js` — change `module.exports = nextConfig` to use the plugin. The full file:

```js
// next.config.js
const createNextIntlPlugin = require('next-intl/plugin');
const withNextIntl = createNextIntlPlugin('./i18n/request.ts');

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'v3b.fal.media',
      },
    ],
  },
  experimental: {
    serverActions: {
      bodySizeLimit: '20mb',
    },
  },
  async redirects() {
    return [];
  },
  async headers() {
    const csp = [
      "default-src 'self'",
      "script-src 'self' 'unsafe-inline' 'unsafe-eval' blob: https://clerk.com https://*.clerk.accounts.dev https://*.clerk.com https://js.stripe.com",
      "style-src 'self' 'unsafe-inline' https://fonts.googleapis.com",
      "font-src 'self' https://fonts.gstatic.com",
      "img-src 'self' data: blob: https:",
      "media-src 'self' blob: https:",
      "connect-src 'self' https://*.supabase.co wss://*.supabase.co https://clerk.com https://*.clerk.accounts.dev https://*.clerk.com https://api.klingai.com https://generativelanguage.googleapis.com https://unpkg.com https://cdn.jsdelivr.net",
      "frame-src https://clerk.com https://*.clerk.accounts.dev https://*.clerk.com https://js.stripe.com",
      "object-src 'none'",
      "worker-src 'self' blob:",
      "base-uri 'self'",
      "form-action 'self' https://www.liqpay.ua",
      "frame-ancestors 'none'",
      "upgrade-insecure-requests",
    ].join('; ');

    const securityHeaders = [
      { key: 'Content-Security-Policy', value: csp },
      { key: 'X-DNS-Prefetch-Control', value: 'on' },
      { key: 'X-Content-Type-Options', value: 'nosniff' },
      { key: 'X-Frame-Options', value: 'SAMEORIGIN' },
      { key: 'X-XSS-Protection', value: '1; mode=block' },
      { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
      { key: 'Permissions-Policy', value: 'camera=(), microphone=(), geolocation=()' },
      { key: 'Strict-Transport-Security', value: 'max-age=63072000; includeSubDomains; preload' },
    ];

    return [
      {
        source: '/(.*)',
        headers: securityHeaders,
      },
      {
        source: '/api/stripe/webhook',
        headers: [{ key: 'x-middleware-skip', value: '1' }],
      },
    ];
  },
};

module.exports = withNextIntl(nextConfig);
```

- [ ] **Step 2: Commit**

```bash
git add next.config.js
git commit -m "feat(i18n): wire next-intl plugin into next.config.js"
```

---

## Task 4: Create messages/en.json

**Files:**
- Create: `messages/en.json`

- [ ] **Step 1: Create the file**

```json
{
  "navbar": {
    "explore": "Explore",
    "image": "Image",
    "video": "Video",
    "pricing": "Pricing",
    "viewProfile": "View profile",
    "settings": "Settings",
    "signOut": "Sign out",
    "credits": "{count} credits",
    "mobileHome": "Home",
    "mobileImage": "Image",
    "mobileVideo": "Video"
  },
  "home": {
    "badge": "AI Creative Studio",
    "heroLine1": "Turn ideas into",
    "cycleWord0": "images",
    "cycleWord1": "videos",
    "cycleWord2": "audio",
    "heroLine3": "using",
    "subtitle": "Infrastructure for AI Video & Image Gen",
    "sectionImageBadge": "Image",
    "sectionImageTitle": "Generate images",
    "sectionImageLink": "Try Image",
    "sectionVideoBadge": "Video",
    "sectionVideoTitle": "Generate videos",
    "sectionVideoLink": "Try Video",
    "tryPrompt": "Try prompt"
  },
  "gallery": {
    "title": "Community Gallery",
    "subtitle": "Created by our users",
    "seeAll": "See all ›",
    "tryPrompt": "✦ Try prompt"
  },
  "pricing": {
    "badge": "Simple pricing",
    "title": "Create more,",
    "titleAccent": "think less",
    "subtitle": "One credit system across images, video, and audio. Upgrade or cancel anytime.",
    "mostPopular": "Most popular",
    "creditsPerMonth": "credits / month",
    "perMonth": "/mo",
    "footerNote": "No contracts · Cancel anytime ·",
    "enterprisePlan": "Enterprise plan →",
    "loading": "Loading…"
  },
  "footer": {
    "tagline": "Create anything with AI",
    "badge": "AI Creative Studio",
    "poweredBy": "Powered by AI"
  },
  "privacy": {
    "badge": "Legal",
    "title": "Privacy Policy",
    "lastUpdated": "Last updated: January 1, 2026",
    "intro": "This Privacy Policy describes how Keyo Studio (\"we\", \"us\", or \"our\") collects, uses, and shares information about you when you use our AI creative platform at keyo.studio.",
    "sections": [
      {
        "title": "1. Information We Collect",
        "body": "We collect information you provide directly when you create an account, such as your name, email address, and payment details. We also collect data generated by your use of our services, including prompts, generated content, usage logs, and device/browser information."
      },
      {
        "title": "2. How We Use Your Information",
        "body": "We use your data to provide and improve our AI generation services, process payments, send transactional and promotional communications (where permitted), ensure platform security, and comply with legal obligations. We do not sell your personal data to third parties."
      },
      {
        "title": "3. Data Retention",
        "body": "We retain your account data for as long as your account is active or as needed to provide services. Generated content may be stored temporarily for delivery and quality purposes. You can request deletion of your data at any time by contacting us."
      },
      {
        "title": "4. Cookies and Tracking",
        "body": "We use cookies and similar tracking technologies to maintain your session, remember preferences, and analyse platform usage. You can control cookie settings through your browser or our cookie consent banner. Disabling certain cookies may affect functionality."
      },
      {
        "title": "5. Third-Party Services",
        "body": "Our platform integrates third-party AI model providers, payment processors (Stripe), analytics tools, and authentication providers (Clerk). These services operate under their own privacy policies. We share only the minimum data necessary for them to function."
      },
      {
        "title": "6. Your Rights",
        "body": "Depending on your location, you may have rights to access, correct, delete, or restrict processing of your personal data, as well as the right to data portability. To exercise any of these rights, contact us at privacy@keyo.studio."
      },
      {
        "title": "7. Security",
        "body": "We implement industry-standard security measures including encryption in transit (TLS), secure credential storage, and regular security reviews. No method of transmission over the internet is 100% secure; we cannot guarantee absolute security."
      },
      {
        "title": "8. Children's Privacy",
        "body": "Keyo Studio is not directed at children under the age of 16. We do not knowingly collect personal data from children. If you believe a child has provided us with personal data, please contact us immediately."
      },
      {
        "title": "9. Changes to This Policy",
        "body": "We may update this Privacy Policy from time to time. We will notify you of significant changes by email or a prominent notice on the platform. Continued use of Keyo Studio after changes constitutes acceptance of the updated policy."
      },
      {
        "title": "10. Contact",
        "body": "If you have questions about this Privacy Policy, please contact us at privacy@keyo.studio or write to us at Keyo Studio, legal department."
      }
    ]
  },
  "terms": {
    "badge": "Legal",
    "title": "Terms of Service",
    "lastUpdated": "Last updated: January 1, 2026",
    "intro": "Please read these Terms of Service carefully before using Keyo Studio. These terms form a binding legal agreement between you and Keyo Studio regarding your use of our AI creative platform.",
    "sections": [
      {
        "title": "1. Acceptance of Terms",
        "body": "By accessing or using Keyo Studio (\"the Service\"), you agree to be bound by these Terms of Service. If you do not agree with any part of these terms, you may not access the Service. These terms apply to all users, including visitors, registered accounts, and paying subscribers."
      },
      {
        "title": "2. Eligibility",
        "body": "You must be at least 16 years old to use Keyo Studio. By creating an account, you represent that you meet this requirement and that all information you provide is accurate and current. We reserve the right to terminate accounts that we believe are operated by minors."
      },
      {
        "title": "3. Account Responsibilities",
        "body": "You are responsible for safeguarding your account credentials and for all activity that occurs under your account. You must notify us immediately of any unauthorised use. We are not liable for any losses arising from your failure to keep your credentials secure."
      },
      {
        "title": "4. Permitted Use",
        "body": "Keyo Studio is provided for lawful creative and professional use only. You may use the Service to generate images, videos, and audio for personal or commercial purposes, subject to credit limits and applicable plan restrictions. You may not use the Service to generate illegal, harmful, deceptive, or rights-infringing content."
      },
      {
        "title": "5. Prohibited Content",
        "body": "You may not use Keyo Studio to generate content that: (a) depicts or promotes illegal activities; (b) contains sexual content involving minors; (c) infringes any intellectual property rights; (d) is used to deceive, defraud, or harass individuals; or (e) violates the usage policies of underlying AI model providers. Violations may result in immediate account termination."
      },
      {
        "title": "6. Ownership of Generated Content",
        "body": "Subject to compliance with these Terms, you retain ownership of content you generate using the Service. We do not claim rights over your outputs. However, you grant us a limited licence to store and process your prompts and outputs for the purpose of delivering the Service and improving model quality."
      },
      {
        "title": "7. Subscriptions and Payments",
        "body": "Paid plans are billed on a recurring basis as described at checkout. All fees are non-refundable except as required by law or as explicitly stated in our refund policy. We reserve the right to change pricing with 30 days' notice. Continued use of the Service after a price change constitutes acceptance."
      },
      {
        "title": "8. Service Availability",
        "body": "We aim to provide a reliable service but do not guarantee uninterrupted access. We may modify, suspend, or discontinue features at any time with reasonable notice. We are not liable for any downtime, data loss, or interruption caused by circumstances beyond our reasonable control."
      },
      {
        "title": "9. Intellectual Property",
        "body": "The Keyo Studio platform, including its design, code, branding, and underlying AI pipeline infrastructure, is owned by us and protected by intellectual property law. You may not copy, reverse-engineer, resell, or create derivative works of the platform itself without our written consent."
      },
      {
        "title": "10. Disclaimers and Limitation of Liability",
        "body": "The Service is provided \"as is\" without warranties of any kind. To the maximum extent permitted by law, we disclaim all implied warranties. Our total liability for any claim arising from these Terms shall not exceed the amount you paid us in the 12 months preceding the claim."
      },
      {
        "title": "11. Termination",
        "body": "We may suspend or terminate your account at any time for violation of these Terms or for any other reason with reasonable notice. You may terminate your account at any time through account settings. Provisions that by their nature should survive termination shall do so."
      },
      {
        "title": "12. Governing Law",
        "body": "These Terms are governed by applicable law. Any disputes shall be resolved in the competent courts of the jurisdiction in which Keyo Studio operates. If any provision of these Terms is found unenforceable, the remaining provisions continue in full force."
      },
      {
        "title": "13. Changes to Terms",
        "body": "We may revise these Terms from time to time. We will provide at least 14 days' notice of material changes via email or in-app notification. Continued use of the Service after the effective date constitutes your acceptance of the revised Terms."
      },
      {
        "title": "14. Contact",
        "body": "For questions about these Terms, contact us at legal@keyo.studio."
      }
    ]
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add messages/en.json
git commit -m "feat(i18n): add English translation file"
```

---

## Task 5: Create messages/ua.json

**Files:**
- Create: `messages/ua.json`

- [ ] **Step 1: Create the file**

```json
{
  "navbar": {
    "explore": "Огляд",
    "image": "Зображення",
    "video": "Відео",
    "pricing": "Ціни",
    "viewProfile": "Профіль",
    "settings": "Налаштування",
    "signOut": "Вийти",
    "credits": "{count} кредитів",
    "mobileHome": "Головна",
    "mobileImage": "Зображення",
    "mobileVideo": "Відео"
  },
  "home": {
    "badge": "AI Творча Студія",
    "heroLine1": "Перетворюй ідеї на",
    "cycleWord0": "зображення",
    "cycleWord1": "відео",
    "cycleWord2": "аудіо",
    "heroLine3": "за допомогою",
    "subtitle": "Платформа для AI генерації відео та зображень",
    "sectionImageBadge": "Зображення",
    "sectionImageTitle": "Генерація зображень",
    "sectionImageLink": "Спробувати",
    "sectionVideoBadge": "Відео",
    "sectionVideoTitle": "Генерація відео",
    "sectionVideoLink": "Спробувати",
    "tryPrompt": "Спробувати"
  },
  "gallery": {
    "title": "Спільна Галерея",
    "subtitle": "Створено нашими користувачами",
    "seeAll": "Всі ›",
    "tryPrompt": "✦ Спробувати"
  },
  "pricing": {
    "badge": "Проста оплата",
    "title": "Створюй більше,",
    "titleAccent": "думай менше",
    "subtitle": "Єдина система кредитів для зображень, відео та аудіо. Підвищуй або скасовуй підписку будь-коли.",
    "mostPopular": "Найпопулярніший",
    "creditsPerMonth": "кредитів / місяць",
    "perMonth": "/міс",
    "footerNote": "Без контрактів · Скасування будь-коли ·",
    "enterprisePlan": "Корпоративний план →",
    "loading": "Завантаження…"
  },
  "footer": {
    "tagline": "Створюй будь-що за допомогою AI",
    "badge": "AI Творча Студія",
    "poweredBy": "Працює на AI"
  },
  "privacy": {
    "badge": "Правові документи",
    "title": "Політика конфіденційності",
    "lastUpdated": "Останнє оновлення: 1 січня 2026 р.",
    "intro": "Ця Політика конфіденційності описує, як Keyo Studio («ми», «нас» або «наш») збирає, використовує та розкриває інформацію про вас, коли ви використовуєте нашу платформу AI творчості на keyo.studio.",
    "sections": [
      {
        "title": "1. Інформація, яку ми збираємо",
        "body": "Ми збираємо інформацію, яку ви надаєте безпосередньо під час створення облікового запису, зокрема ваше ім'я, адресу електронної пошти та платіжні дані. Також ми збираємо дані, що генеруються під час використання наших сервісів, включно із запитами, згенерованим контентом, журналами використання та інформацією про пристрій і браузер."
      },
      {
        "title": "2. Як ми використовуємо вашу інформацію",
        "body": "Ми використовуємо ваші дані для надання та покращення наших сервісів AI генерації, обробки платежів, надсилання транзакційних і рекламних повідомлень (де це дозволено), забезпечення безпеки платформи та дотримання юридичних зобов'язань. Ми не продаємо ваші персональні дані третім сторонам."
      },
      {
        "title": "3. Зберігання даних",
        "body": "Ми зберігаємо дані вашого облікового запису протягом усього часу його активності або поки це необхідно для надання послуг. Згенерований контент може тимчасово зберігатися для доставки та контролю якості. Ви можете будь-коли подати запит на видалення своїх даних, зв'язавшись з нами."
      },
      {
        "title": "4. Файли cookie та відстеження",
        "body": "Ми використовуємо файли cookie та аналогічні технології відстеження для підтримки вашої сесії, запам'ятовування налаштувань та аналізу використання платформи. Ви можете керувати налаштуваннями файлів cookie через свій браузер або наш банер згоди. Відключення певних cookie може вплинути на функціональність."
      },
      {
        "title": "5. Сторонні сервіси",
        "body": "Наша платформа інтегрує сторонніх постачальників AI-моделей, платіжні системи, аналітичні інструменти та постачальників аутентифікації (Clerk). Ці сервіси діють відповідно до власних політик конфіденційності. Ми передаємо лише мінімально необхідні дані для їх функціонування."
      },
      {
        "title": "6. Ваші права",
        "body": "Залежно від вашого місцезнаходження, ви можете мати права на доступ, виправлення, видалення або обмеження обробки ваших персональних даних, а також право на перенесення даних. Для реалізації будь-якого з цих прав зв'яжіться з нами за адресою privacy@keyo.studio."
      },
      {
        "title": "7. Безпека",
        "body": "Ми застосовуємо стандартні заходи безпеки, включно з шифруванням при передачі (TLS), безпечним зберіганням облікових даних та регулярними перевірками безпеки. Жоден метод передачі через Інтернет не є 100% безпечним; ми не можемо гарантувати абсолютну безпеку."
      },
      {
        "title": "8. Конфіденційність дітей",
        "body": "Keyo Studio не призначений для дітей до 16 років. Ми свідомо не збираємо персональні дані дітей. Якщо ви вважаєте, що дитина надала нам свої персональні дані, будь ласка, негайно зв'яжіться з нами."
      },
      {
        "title": "9. Зміни до цієї Політики",
        "body": "Ми можемо час від часу оновлювати цю Політику конфіденційності. Ми повідомимо вас про суттєві зміни електронною поштою або помітним повідомленням на платформі. Продовження використання Keyo Studio після внесення змін означає прийняття оновленої політики."
      },
      {
        "title": "10. Контакти",
        "body": "Якщо у вас є запитання щодо цієї Політики конфіденційності, будь ласка, зв'яжіться з нами за адресою privacy@keyo.studio або напишіть до юридичного відділу Keyo Studio."
      }
    ]
  },
  "terms": {
    "badge": "Правові документи",
    "title": "Умови надання послуг",
    "lastUpdated": "Останнє оновлення: 1 січня 2026 р.",
    "intro": "Будь ласка, уважно прочитайте ці Умови надання послуг перед використанням Keyo Studio. Ці умови є юридично обов'язковою угодою між вами та Keyo Studio щодо використання нашої AI-платформи.",
    "sections": [
      {
        "title": "1. Прийняття умов",
        "body": "Отримуючи доступ або використовуючи Keyo Studio («Сервіс»), ви погоджуєтеся дотримуватися цих Умов надання послуг. Якщо ви не погоджуєтеся з будь-якою частиною цих умов, ви не можете використовувати Сервіс. Ці умови поширюються на всіх користувачів, включаючи відвідувачів, зареєстровані облікові записи та платних підписників."
      },
      {
        "title": "2. Право на використання",
        "body": "Вам повинно бути не менше 16 років для використання Keyo Studio. Створюючи обліковий запис, ви підтверджуєте, що відповідаєте цій вимозі та що вся надана вами інформація є точною та актуальною. Ми залишаємо за собою право закрити облікові записи, якщо вважаємо, що ними керують неповнолітні."
      },
      {
        "title": "3. Відповідальність за обліковий запис",
        "body": "Ви несете відповідальність за захист облікових даних та за всі дії, що здійснюються у вашому обліковому записі. Ви зобов'язані негайно повідомити нас про будь-яке несанкціоноване використання. Ми не несемо відповідальності за будь-які втрати, що виникли внаслідок неналежного захисту облікових даних."
      },
      {
        "title": "4. Дозволене використання",
        "body": "Keyo Studio надається виключно для законного творчого та професійного використання. Ви можете використовувати Сервіс для генерації зображень, відео та аудіо в особистих або комерційних цілях з урахуванням лімітів кредитів та обмежень відповідного плану. Вам заборонено використовувати Сервіс для генерації незаконного, шкідливого, оманливого або контенту, що порушує авторські права."
      },
      {
        "title": "5. Заборонений контент",
        "body": "Вам забороняється використовувати Keyo Studio для генерації контенту, який: (а) зображує або пропагує незаконну діяльність; (б) містить сексуальний контент за участю неповнолітніх; (в) порушує будь-які права інтелектуальної власності; (г) використовується для обману або переслідування осіб; або (ґ) порушує правила використання базових постачальників AI-моделей. Порушення може призвести до негайного закриття облікового запису."
      },
      {
        "title": "6. Право власності на згенерований контент",
        "body": "За умови дотримання цих Умов, ви зберігаєте право власності на контент, який генеруєте за допомогою Сервісу. Ми не претендуємо на права щодо ваших результатів. Однак ви надаєте нам обмежену ліцензію на зберігання та обробку ваших запитів і результатів з метою надання Сервісу та покращення якості моделей."
      },
      {
        "title": "7. Підписки та платежі",
        "body": "Платні плани оплачуються на регулярній основі, як описано під час оформлення замовлення. Усі платежі не підлягають поверненню, якщо інше не вимагається законом або не зазначено в нашій політиці повернення коштів. Ми залишаємо за собою право змінювати ціни з попередженням за 30 днів."
      },
      {
        "title": "8. Доступність сервісу",
        "body": "Ми прагнемо надавати надійний сервіс, але не гарантуємо безперебійний доступ. Ми можемо змінювати, призупиняти або припиняти функції будь-коли з розумним попередженням. Ми не несемо відповідальності за будь-які простої або втрату даних, спричинені обставинами поза нашим контролем."
      },
      {
        "title": "9. Інтелектуальна власність",
        "body": "Платформа Keyo Studio, включаючи її дизайн, код, брендинг та базову AI-інфраструктуру, належить нам і захищена законодавством про інтелектуальну власність. Вам забороняється копіювати, декомпілювати, перепродавати або створювати похідні твори самої платформи без нашої письмової згоди."
      },
      {
        "title": "10. Відмова від відповідальності та обмеження відповідальності",
        "body": "Сервіс надається «як є» без будь-яких гарантій. У максимально дозволеному законом обсязі ми відмовляємося від усіх неявних гарантій. Наша загальна відповідальність за будь-яку претензію, що виникла з цих Умов, не перевищує суму, сплачену вами нам протягом 12 місяців, що передують претензії."
      },
      {
        "title": "11. Припинення дії",
        "body": "Ми можемо призупинити або закрити ваш обліковий запис у будь-який час за порушення цих Умов або з будь-якої іншої причини з розумним попередженням. Ви можете закрити свій обліковий запис у будь-який час через налаштування облікового запису."
      },
      {
        "title": "12. Застосовне законодавство",
        "body": "Ці Умови регулюються чинним законодавством. Будь-які спори вирішуються у компетентних судах юрисдикції, в якій діє Keyo Studio. Якщо будь-яке положення цих Умов визнається невиконуваним, решта положень продовжують діяти в повній силі."
      },
      {
        "title": "13. Зміни до Умов",
        "body": "Ми можемо час від часу переглядати ці Умови. Ми надамо попередження щонайменше за 14 днів про суттєві зміни через електронну пошту або сповіщення в додатку. Продовження використання Сервісу після набрання чинності змін означає прийняття переглянутих Умов."
      },
      {
        "title": "14. Контакти",
        "body": "З питань щодо цих Умов звертайтеся до нас за адресою legal@keyo.studio."
      }
    ]
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add messages/ua.json
git commit -m "feat(i18n): add Ukrainian translation file"
```

---

## Task 6: Update middleware.ts

**Files:**
- Modify: `middleware.ts`

- [ ] **Step 1: Replace the file entirely**

```ts
// middleware.ts
import createMiddleware from 'next-intl/middleware';
import { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server';
import { routing } from './i18n/routing';

const intlMiddleware = createMiddleware(routing);

const isPublicRoute = createRouteMatcher([
  '/:locale/sign-in(.*)',
  '/:locale/sign-up(.*)',
  '/:locale/sso-callback(.*)',
  '/:locale',
  '/:locale/image',
  '/:locale/video',
  '/:locale/audio',
  '/:locale/pricing',
  '/:locale/privacy',
  '/:locale/terms',
  '/api/liqpay/webhook(.*)',
  '/api/community-gallery(.*)',
]);

export default clerkMiddleware(async (auth, request) => {
  if (!isPublicRoute(request)) {
    await auth.protect();
  }
  return intlMiddleware(request);
});

export const config = {
  matcher: [
    '/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)',
    '/(api|trpc)(.*)',
  ],
};
```

- [ ] **Step 2: Commit**

```bash
git add middleware.ts
git commit -m "feat(i18n): compose Clerk + next-intl in middleware"
```

---

## Task 7: Restructure app/ — root layout + locale layout

**Files:**
- Modify: `app/layout.tsx`
- Create: `app/[locale]/layout.tsx`

- [ ] **Step 1: Strip `app/layout.tsx` to a minimal shell**

```tsx
// app/layout.tsx
import type { ReactNode } from 'react';
import './globals.css';

export default function RootLayout({ children }: { children: ReactNode }) {
  return children;
}
```

- [ ] **Step 2: Create `app/[locale]/layout.tsx`**

```tsx
// app/[locale]/layout.tsx
import type { Metadata } from 'next';
import type { ReactNode } from 'react';
import { Syne, Onest } from 'next/font/google';
import { notFound } from 'next/navigation';
import { ClerkProvider } from '@clerk/nextjs';
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';
import Providers from '@/components/layout/Providers';
import CookieConsentBanner from '@/components/layout/CookieConsent';
import { routing } from '@/i18n/routing';

const syne = Syne({
  subsets: ['latin'],
  weight: ['800'],
  variable: '--font-syne',
  display: 'swap',
});

const onest = Onest({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600'],
  variable: '--font-dm',
  display: 'swap',
});

export const metadata: Metadata = {
  metadataBase: new URL('https://keyo.studio'),
  title: {
    default: 'Keyo Studio — AI Creative Platform',
    template: '%s | Keyo Studio',
  },
  description: 'Infrastructure for AI Video & Image Gen. Generate images, videos and audio using top AI models in one place.',
  keywords: ['AI image generation', 'AI video generation', 'AI audio generation', 'AI creative platform', 'text to image', 'text to video'],
  icons: {
    icon: '/favicon.png',
    shortcut: '/favicon.png',
    apple: '/favicon.png',
  },
  openGraph: {
    title: 'Keyo Studio — AI Creative Platform',
    description: 'Infrastructure for AI Video & Image Gen. Generate images, videos and audio using top AI models in one place.',
    url: 'https://keyo.studio',
    siteName: 'Keyo Studio',
    images: [{ url: '/ot-banner.png', width: 1200, height: 630, alt: 'Keyo Studio' }],
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
    title: 'Keyo Studio — AI Creative Platform',
    description: 'Infrastructure for AI Video & Image Gen. Generate images, videos and audio using top AI models in one place.',
    images: ['/ot-banner.png'],
  },
  robots: { index: true, follow: true },
  alternates: {
    languages: {
      'en': 'https://keyo.studio/en',
      'uk': 'https://keyo.studio/ua',
      'x-default': 'https://keyo.studio/en',
    },
  },
};

const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Organization',
      '@id': 'https://keyo.studio/#organization',
      name: 'Keyo Studio',
      url: 'https://keyo.studio',
      logo: { '@type': 'ImageObject', url: 'https://keyo.studio/favicon.png' },
      sameAs: [],
    },
    {
      '@type': 'WebSite',
      '@id': 'https://keyo.studio/#website',
      url: 'https://keyo.studio',
      name: 'Keyo Studio',
      description: 'Infrastructure for AI Video & Image Gen',
      publisher: { '@id': 'https://keyo.studio/#organization' },
    },
    {
      '@type': 'SoftwareApplication',
      name: 'Keyo Studio',
      operatingSystem: 'Web',
      applicationCategory: 'MultimediaApplication',
      offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
    },
  ],
};

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export default async function LocaleLayout({
  children,
  params,
}: {
  children: ReactNode;
  params: { locale: string };
}) {
  const { locale } = params;
  if (!(routing.locales as readonly string[]).includes(locale)) notFound();

  const messages = await getMessages();

  return (
    <html lang={locale} translate="no">
      <head>
        <link
          rel="stylesheet"
          href="https://api.fontshare.com/v2/css?f[]=clash-display@400,500,600,700&display=swap"
        />
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
        />
      </head>
      <body className={`${syne.variable} ${onest.variable} font-dm antialiased`}>
        <ClerkProvider
          appearance={{
            variables: {
              colorPrimary: '#532fcf',
              colorBackground: '#0f0f0f',
              colorText: '#f0f0f0',
              colorInputBackground: '#161616',
              colorInputText: '#f0f0f0',
              borderRadius: '0.75rem',
            },
          }}
        >
          <NextIntlClientProvider messages={messages}>
            <Providers>{children}</Providers>
            <CookieConsentBanner />
          </NextIntlClientProvider>
        </ClerkProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add app/layout.tsx app/[locale]/layout.tsx
git commit -m "feat(i18n): add [locale] layout with NextIntlClientProvider"
```

---

## Task 8: Move all pages into app/[locale]/

**Files:** All page/layout files under `app/` (except `layout.tsx`, `globals.css`, `sitemap.ts`)

- [ ] **Step 1: Create subdirectory structure and move files**

Run these commands (they create the target dirs and move the files):

```bash
# Create locale subdirs
mkdir -p app/\[locale\]/pricing app/\[locale\]/privacy app/\[locale\]/terms
mkdir -p app/\[locale\]/dashboard app/\[locale\]/settings
mkdir -p app/\[locale\]/image app/\[locale\]/video app/\[locale\]/audio app/\[locale\]/editor
mkdir -p app/\[locale\]/sign-in app/\[locale\]/sign-up app/\[locale\]/sso-callback

# Move top-level home page
mv app/page.tsx app/\[locale\]/page.tsx

# Move pricing
mv app/pricing/page.tsx app/\[locale\]/pricing/page.tsx
rmdir app/pricing

# Move privacy
mv app/privacy/page.tsx app/\[locale\]/privacy/page.tsx
rmdir app/privacy

# Move terms
mv app/terms/page.tsx app/\[locale\]/terms/page.tsx
rmdir app/terms

# Move dashboard
mv app/dashboard/layout.tsx app/\[locale\]/dashboard/layout.tsx
mv app/dashboard/page.tsx app/\[locale\]/dashboard/page.tsx
rmdir app/dashboard

# Move settings
mv app/settings/layout.tsx app/\[locale\]/settings/layout.tsx
mv app/settings/page.tsx app/\[locale\]/settings/page.tsx
rmdir app/settings

# Move image
mv app/image/layout.tsx app/\[locale\]/image/layout.tsx
mv app/image/page.tsx app/\[locale\]/image/page.tsx
rmdir app/image

# Move video
mv app/video/layout.tsx app/\[locale\]/video/layout.tsx
mv app/video/page.tsx app/\[locale\]/video/page.tsx
rmdir app/video

# Move audio
mv app/audio/layout.tsx app/\[locale\]/audio/layout.tsx
mv app/audio/page.tsx app/\[locale\]/audio/page.tsx
rmdir app/audio

# Move editor
mv app/editor/layout.tsx app/\[locale\]/editor/layout.tsx
mv app/editor/page.tsx app/\[locale\]/editor/page.tsx
rmdir app/editor

# Move sign-in
mv app/sign-in/layout.tsx app/\[locale\]/sign-in/layout.tsx
mv app/sign-in/page.tsx app/\[locale\]/sign-in/page.tsx
rmdir app/sign-in

# Move sign-up
mv app/sign-up/layout.tsx app/\[locale\]/sign-up/layout.tsx
mv app/sign-up/page.tsx app/\[locale\]/sign-up/page.tsx
rmdir app/sign-up

# Move sso-callback
mv app/sso-callback/page.tsx app/\[locale\]/sso-callback/page.tsx
rmdir app/sso-callback
```

- [ ] **Step 2: Fix relative imports in `app/[locale]/page.tsx`** (was `app/page.tsx`, imports were `../components/...`)

Open `app/[locale]/page.tsx` and change the import block at the top from:
```tsx
import Navbar from '../components/layout/Navbar';
import Footer from '../components/layout/Footer';
import ToolCard from '../components/home/ToolCard';
import CommunityGallery from '../components/home/CommunityGallery';
```
to:
```tsx
import Navbar from '@/components/layout/Navbar';
import Footer from '@/components/layout/Footer';
import ToolCard from '@/components/home/ToolCard';
import CommunityGallery from '@/components/home/CommunityGallery';
```

- [ ] **Step 3: Fix relative imports in privacy and terms** (were `../../components/...`, now `../../../components/...` — switch to `@/`)

In `app/[locale]/privacy/page.tsx`, replace:
```tsx
import Navbar from '../../components/layout/Navbar';
import Footer from '../../components/layout/Footer';
```
with:
```tsx
import Navbar from '@/components/layout/Navbar';
import Footer from '@/components/layout/Footer';
```

Same change in `app/[locale]/terms/page.tsx`.

- [ ] **Step 4: Fix Clerk path props in sign-in page**

In `app/[locale]/sign-in/page.tsx`, replace the `<SignIn>` props:
```tsx
// Before
<SignIn
  routing="path"
  path="/sign-in"
  fallbackRedirectUrl="/"
  signUpUrl="/sign-up"
  ...
/>
```
```tsx
// After — params is available since we're in a [locale] segment
// Add params prop to the component:
export default function Page({ params }: { params: { locale: string } }) {
  return (
    <div className="min-h-screen bg-[var(--bg)] flex items-center justify-center p-4">
      <SignIn
        routing="path"
        path={`/${params.locale}/sign-in`}
        fallbackRedirectUrl={`/${params.locale}`}
        signUpUrl={`/${params.locale}/sign-up`}
        appearance={{
          variables: {
            colorPrimary: '#532fcf',
            colorBackground: '#0f0f0f',
            colorText: '#f0f0f0',
            colorInputBackground: '#111111',
            colorInputText: '#f0f0f0',
            borderRadius: '0.75rem',
          },
          elements: {
            rootBox: 'shadow-2xl',
            card: 'bg-[#0f0f0f] border border-white/[0.08] shadow-2xl',
            formButtonPrimary: 'bg-accent hover:brightness-110 font-dm font-semibold',
            footerActionLink: 'text-accent',
          },
        }}
      />
    </div>
  );
}
```

- [ ] **Step 5: Fix sign-up page the same way** — apply the same `params` pattern, changing `path="/sign-up"` to `path={`/${params.locale}/sign-up`}` and `signInUrl="/sign-in"` to `signInUrl={`/${params.locale}/sign-in`}`.

- [ ] **Step 6: Fix sso-callback redirect URLs**

In `app/[locale]/sso-callback/page.tsx`:
```tsx
'use client';
import { AuthenticateWithRedirectCallback } from '@clerk/nextjs';

export default function SSOCallbackPage({ params }: { params: { locale: string } }) {
  return (
    <div className="min-h-screen flex items-center justify-center" style={{ background: 'var(--bg)' }}>
      <AuthenticateWithRedirectCallback
        signInFallbackRedirectUrl={`/${params.locale}`}
        signUpFallbackRedirectUrl={`/${params.locale}`}
      />
    </div>
  );
}
```

- [ ] **Step 7: Fix dashboard and settings imports** (were `../../components/...`)

In `app/[locale]/dashboard/page.tsx` and `app/[locale]/settings/page.tsx`, change any `../../components/...` imports to `@/components/...`.

- [ ] **Step 8: Verify the build compiles**

```bash
npm run build 2>&1 | tail -30
```

Expected: no "Module not found" errors. There may be runtime errors about missing translations (fixed in later tasks) but the build should not crash on imports.

- [ ] **Step 9: Commit**

```bash
git add app/
git commit -m "feat(i18n): move all pages into app/[locale]/ and fix imports"
```

---

## Task 9: Create LocaleSwitcher component

**Files:**
- Create: `components/layout/LocaleSwitcher.tsx`

- [ ] **Step 1: Create the component**

```tsx
// components/layout/LocaleSwitcher.tsx
'use client';
import { useLocale } from 'next-intl';
import { usePathname, useRouter } from '@/i18n/navigation';
import { useTransition } from 'react';

export default function LocaleSwitcher() {
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();
  const [isPending, startTransition] = useTransition();

  const toggle = () => {
    const next = locale === 'en' ? 'ua' : 'en';
    startTransition(() => {
      router.replace(pathname, { locale: next });
    });
  };

  return (
    <button
      onClick={toggle}
      disabled={isPending}
      className="font-dm hidden md:inline-flex items-center gap-[5px] transition-all duration-150"
      style={{
        fontSize: '12px',
        fontWeight: 500,
        padding: '5px 10px',
        borderRadius: '20px',
        background: 'rgba(255,255,255,0.04)',
        border: '0.5px solid rgba(255,255,255,0.1)',
        cursor: isPending ? 'wait' : 'pointer',
        color: 'rgba(255,255,255,0.6)',
        opacity: isPending ? 0.5 : 1,
      }}
      aria-label="Switch language"
    >
      <span style={{ opacity: locale === 'en' ? 1 : 0.38, color: locale === 'en' ? 'rgba(190,165,255,0.9)' : undefined }}>EN</span>
      <span style={{ opacity: 0.2 }}>|</span>
      <span style={{ opacity: locale === 'ua' ? 1 : 0.38, color: locale === 'ua' ? 'rgba(190,165,255,0.9)' : undefined }}>UA</span>
    </button>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/layout/LocaleSwitcher.tsx
git commit -m "feat(i18n): add LocaleSwitcher EN|UA toggle component"
```

---

## Task 10: Update Navbar

**Files:**
- Modify: `components/layout/Navbar.tsx`

- [ ] **Step 1: Replace the full file**

```tsx
// components/layout/Navbar.tsx
'use client';
import React, { useState, useEffect, useRef } from 'react';
import { usePathname } from '@/i18n/navigation';
import { Link } from '@/i18n/navigation';
import { useUser, useClerk } from '@clerk/nextjs';
import { useTranslations } from 'next-intl';
import Portal from '../ui/Portal';
import Logo from '../ui/Logo';
import LocaleSwitcher from './LocaleSwitcher';
import { useAuth } from '@/context/AuthContext';

const clerkAppearance = {
  elements: {
    rootBox: 'w-full',
    card: '!shadow-none !border-0 !rounded-none',
    header: '!hidden',
    formFieldInput: { fontSize: '16px' },
    badge: '!bg-[rgba(83,47,207,0.08)] !text-[#7c5cf0] !border-[rgba(83,47,207,0.3)] !rounded-full !font-medium',
  },
};

export default function Navbar() {
  const pathname = usePathname();
  const { isSignedIn, user } = useUser();
  const { signOut } = useClerk();
  const t = useTranslations('navbar');

  const [credits, setCredits] = useState<number | null>(null);
  const [userIsAdmin, setUserIsAdmin] = useState(false);
  const { setShowModal, setAuthMode } = useAuth();
  const setShowAuthModal = setShowModal;
  const [showUserMenu, setShowUserMenu] = useState(false);
  const avatarButtonRef = useRef<HTMLButtonElement>(null);
  const [menuPos, setMenuPos] = useState({ top: 0, right: 0 });

  const fetchCredits = async () => {
    if (!isSignedIn) return;
    try {
      const res = await fetch('/api/user-credits');
      const data = await res.json();
      if (data.credits !== undefined) setCredits(data.credits);
      if (data.isAdmin !== undefined) setUserIsAdmin(data.isAdmin);
    } catch {
      setCredits(null);
    }
  };

  useEffect(() => {
    if (isSignedIn) fetchCredits();
  }, [isSignedIn]);

  useEffect(() => {
    const handler = () => fetchCredits();
    window.addEventListener('credits-updated', handler);
    return () => window.removeEventListener('credits-updated', handler);
  }, [isSignedIn]);

  const navLinks = [
    { name: t('explore'), href: '/' },
    { name: t('image'), href: '/image' },
    { name: t('video'), href: '/video' },
  ];

  const avatarLetter = (
    (user?.firstName?.[0] ?? '') + (user?.lastName?.[0] ?? '')
  ).toUpperCase() || user?.emailAddresses?.[0]?.emailAddress?.[0]?.toUpperCase() || 'K';
  const displayName = user?.fullName ?? user?.firstName ?? user?.emailAddresses?.[0]?.emailAddress ?? 'User';

  return (
    <>
      {/* ── Main navbar ── */}
      <nav
        className="fixed top-0 z-[100]"
        style={{
          left: '16px',
          right: '16px',
          height: '60px',
          background: 'rgba(6,6,6,0.88)',
          backdropFilter: 'blur(24px) saturate(180%)',
          WebkitBackdropFilter: 'blur(24px) saturate(180%)',
          borderRadius: '0 0 16px 16px',
          borderLeft: '0.5px solid rgba(255,255,255,0.05)',
          borderRight: '0.5px solid rgba(255,255,255,0.05)',
          boxShadow: '0 0 0 1px rgba(83,47,207,0.06), 0 8px 40px rgba(0,0,0,0.5)',
        }}
      >
        <div className="h-full px-4 flex items-center justify-between">

          {/* ── Logo ── */}
          <Logo size={23} />

          {/* ── Center nav links ── */}
          <div className="hidden md:flex items-center gap-0.5">
            {navLinks.map((link) => {
              const isActive = pathname === link.href;
              return (
                <Link
                  key={link.href}
                  href={link.href}
                  className={`px-4 py-1.5 font-dm font-[500] text-[13px] transition-all duration-150 ${isActive ? 'nav-active' : 'text-white/38 hover:text-white/80'}`}
                >
                  {link.name}
                </Link>
              );
            })}
          </div>

          {/* ── Right controls ── */}
          <div className="flex items-center gap-2">

            {/* ── Pricing pill ── */}
            <Link
              href="/pricing"
              className="pricing-pill hidden md:inline-flex items-center gap-1.5 font-dm font-[500] text-[12px] transition-colors duration-200"
              style={{
                padding: '5px 11px 5px 8px',
                borderRadius: '20px',
                background: 'rgba(120,80,255,0.15)',
                border: '1px solid rgba(120,80,255,0.3)',
                color: 'rgba(190,165,255,0.9)',
              }}
            >
              <svg width="11" height="11" viewBox="0 0 24 24" fill="currentColor" stroke="none">
                <path d="M12 2l2.4 7.4H22l-6.2 4.5 2.4 7.4L12 17l-6.2 4.3 2.4-7.4L2 9.4h7.6z"/>
              </svg>
              {t('pricing')}
            </Link>

            {/* ── Language switcher ── */}
            <LocaleSwitcher />

            {isSignedIn ? (
              <div>
                <button
                  ref={avatarButtonRef}
                  onClick={() => {
                    if (avatarButtonRef.current) {
                      const r = avatarButtonRef.current.getBoundingClientRect();
                      setMenuPos({ top: r.bottom + 8, right: window.innerWidth - r.right });
                    }
                    setShowUserMenu(v => !v);
                  }}
                  className="w-[30px] h-[30px] rounded-full flex items-center justify-center cursor-pointer shrink-0 transition-all duration-200 hover:border-[rgba(120,80,255,0.5)]"
                  style={{ background: 'rgba(120,80,255,0.15)', border: '1px solid rgba(120,80,255,0.3)' }}
                  aria-label="User menu"
                >
                  <span className="font-dm font-[600] leading-none select-none" style={{ fontSize: avatarLetter.length > 1 ? '10px' : '12px', color: 'rgba(190,165,255,0.9)' }}>
                    {avatarLetter}
                  </span>
                </button>

                {showUserMenu && (
                  <Portal>
                    <div className="fixed inset-0 z-[300]" onClick={() => setShowUserMenu(false)} />
                    <div
                      className="fixed z-[301] rounded-2xl p-2 min-w-[210px] shadow-2xl"
                      style={{ top: menuPos.top, right: menuPos.right, background: 'rgba(10,10,10,0.96)', backdropFilter: 'blur(24px)', WebkitBackdropFilter: 'blur(24px)', border: '1px solid rgba(255,255,255,0.07)' }}
                      onClick={(e) => e.stopPropagation()}
                    >
                      <div className="px-3 py-2.5 flex items-center gap-2.5">
                        <div
                          className="w-[30px] h-[30px] rounded-full flex items-center justify-center shrink-0"
                          style={{ background: 'linear-gradient(135deg, rgba(83,47,207,0.5), rgba(140,90,255,0.3))', border: '1px solid rgba(120,80,255,0.3)' }}
                        >
                          <span className="font-syne font-[800] text-white leading-none" style={{ fontSize: avatarLetter.length > 1 ? '9px' : '11px' }}>{avatarLetter}</span>
                        </div>
                        <div className="overflow-hidden">
                          <p className="font-dm font-[500] text-[12px] text-white truncate leading-tight">{displayName}</p>
                          <div className="flex items-center gap-1 mt-0.5">
                            <span style={{ color: 'rgba(120,80,255,0.8)', fontSize: '9px' }}>✦</span>
                            <span className="font-dm text-[11px] text-white/30">
                              {credits !== null ? t('credits', { count: credits }) : '···'}
                            </span>
                          </div>
                        </div>
                      </div>

                      <div className="h-[1px] mx-1 my-1.5" style={{ background: 'rgba(255,255,255,0.05)' }} />

                      <Link href="/dashboard" onClick={() => setShowUserMenu(false)} className="flex items-center gap-2.5 w-full px-3 py-2 rounded-xl text-[12px] text-white/40 hover:text-white hover:bg-white/[0.04] transition-all duration-150 font-dm">
                        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
                        {t('viewProfile')}
                      </Link>
                      <Link href="/settings" onClick={() => setShowUserMenu(false)} className="flex items-center gap-2.5 w-full px-3 py-2 rounded-xl text-[12px] text-white/40 hover:text-white hover:bg-white/[0.04] transition-all duration-150 font-dm">
                        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
                        {t('settings')}
                      </Link>

                      <div className="h-[1px] mx-1 my-1.5" style={{ background: 'rgba(255,255,255,0.05)' }} />

                      <button
                        onClick={() => { setShowUserMenu(false); signOut({ redirectUrl: '/' }); }}
                        className="flex items-center gap-2.5 w-full px-3 py-2 rounded-xl text-[12px] text-white/40 hover:text-red-400 hover:bg-red-500/[0.06] transition-all duration-150 font-dm"
                      >
                        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
                        {t('signOut')}
                      </button>
                    </div>
                  </Portal>
                )}
              </div>
            ) : (
              <button
                onClick={() => setShowAuthModal(true)}
                className="w-[30px] h-[30px] rounded-full flex items-center justify-center cursor-pointer shrink-0 transition-all duration-200 hover:opacity-75"
                style={{ background: 'rgba(255,255,255,0.06)', border: '0.5px solid rgba(255,255,255,0.1)' }}
                aria-label="Sign in or create account"
              >
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="white" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>
                </svg>
              </button>
            )}
          </div>
        </div>
      </nav>

      {/* ── Mobile bottom nav ── */}
      <nav
        className="md:hidden fixed bottom-0 left-0 right-0 z-[100]"
        aria-label="Mobile navigation"
        style={{
          height: 'calc(65px + env(safe-area-inset-bottom, 0px))',
          background: 'rgba(6,6,6,0.96)',
          backdropFilter: 'blur(28px) saturate(180%)',
          WebkitBackdropFilter: 'blur(28px) saturate(180%)',
          borderTop: '0.5px solid rgba(255,255,255,0.07)',
          boxShadow: '0 -8px 40px rgba(0,0,0,0.6)',
          WebkitTapHighlightColor: 'transparent',
        }}
      >
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', height: '65px', overflow: 'visible' }}>

          <Link href="/image"
            className="flex flex-col items-center justify-center gap-[3px] active:opacity-60 transition-opacity"
            style={{ color: pathname === '/image' ? 'rgba(170,140,255,0.95)' : 'rgba(255,255,255,0.3)', touchAction: 'manipulation', minHeight: '44px', WebkitTapHighlightColor: 'transparent' }}>
            <svg width="21" height="21" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
              <rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/>
            </svg>
            <span className="font-dm text-[10px] font-[500]">{t('mobileImage')}</span>
          </Link>

          <div className="flex flex-col items-center justify-center relative" style={{ top: '-14px', WebkitTapHighlightColor: 'transparent' }}>
            <Link href="/" aria-label="Home"
              className="w-[52px] h-[52px] flex items-center justify-center rounded-2xl mb-1 active:opacity-75 transition-opacity"
              style={{ background: pathname === '/' ? 'linear-gradient(135deg, #532fcf 0%, #7c5cf0 100%)' : 'linear-gradient(135deg, #3d2299 0%, #5a3fd4 100%)', boxShadow: '0 4px 24px rgba(83,47,207,0.55), 0 0 0 1px rgba(120,80,255,0.3)', touchAction: 'manipulation', WebkitTapHighlightColor: 'transparent' }}>
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="rgba(220,200,255,1)" strokeWidth="1.7" strokeLinecap="round" strokeLinejoin="round">
                <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/>
              </svg>
            </Link>
            <span className="font-dm text-[10px] font-[500]" style={{ color: pathname === '/' ? 'rgba(170,140,255,0.95)' : 'rgba(255,255,255,0.3)' }}>{t('mobileHome')}</span>
          </div>

          <Link href="/video"
            className="flex flex-col items-center justify-center gap-[3px] active:opacity-60 transition-opacity"
            style={{ color: pathname === '/video' ? 'rgba(170,140,255,0.95)' : 'rgba(255,255,255,0.3)', touchAction: 'manipulation', minHeight: '44px', WebkitTapHighlightColor: 'transparent' }}>
            <svg width="21" height="21" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
              <polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2"/>
            </svg>
            <span className="font-dm text-[10px] font-[500]">{t('mobileVideo')}</span>
          </Link>

        </div>
      </nav>
    </>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/layout/Navbar.tsx
git commit -m "feat(i18n): translate Navbar, add LocaleSwitcher"
```

---

## Task 11: Update Footer

**Files:**
- Modify: `components/layout/Footer.tsx`

- [ ] **Step 1: Add translations**

Replace the three hardcoded strings and add `useTranslations`. The only change is adding `'use client'`, importing `useTranslations` and `Link` from navigation, and replacing text values. Full file:

```tsx
// components/layout/Footer.tsx
'use client';
import React from 'react';
import { Link } from '@/i18n/navigation';
import { useTranslations } from 'next-intl';
import Logo from '../ui/Logo';

const socials = [
  { label: 'X', href: 'https://x.com', path: 'M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-4.714-6.231-5.401 6.231H2.744l7.73-8.835L1.254 2.25H8.08l4.259 5.63zm-1.161 17.52h1.833L7.084 4.126H5.117z' },
  { label: 'YouTube', href: 'https://youtube.com', path: 'M22.54 6.42a2.78 2.78 0 0 0-1.95-1.96C18.88 4 12 4 12 4s-6.88 0-8.59.46a2.78 2.78 0 0 0-1.95 1.96A29 29 0 0 0 1 12a29 29 0 0 0 .46 5.58A2.78 2.78 0 0 0 3.41 19.6C5.12 20 12 20 12 20s6.88 0 8.59-.46a2.78 2.78 0 0 0 1.95-1.95A29 29 0 0 0 23 12a29 29 0 0 0-.46-5.58zM9.75 15.02V8.98L15.5 12z' },
  { label: 'Instagram', href: 'https://instagram.com', path: 'M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 1 0 0 12.324 6.162 6.162 0 0 0 0-12.324zM12 16a4 4 0 1 1 0-8 4 4 0 0 1 0 8zm6.406-11.845a1.44 1.44 0 1 0 0 2.881 1.44 1.44 0 0 0 0-2.881z' },
  { label: 'TikTok', href: 'https://tiktok.com', path: 'M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-2.88 2.5 2.89 2.89 0 0 1-2.89-2.89 2.89 2.89 0 0 1 2.89-2.89c.28 0 .54.04.79.1V9.01a6.32 6.32 0 0 0-.79-.05 6.34 6.34 0 0 0-6.34 6.34 6.34 6.34 0 0 0 6.34 6.34 6.34 6.34 0 0 0 6.33-6.34l-.01-8.83a8.18 8.18 0 0 0 4.78 1.52V4.56a4.85 4.85 0 0 1-1-.13z' },
  { label: 'LinkedIn', href: 'https://linkedin.com', path: 'M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z' },
];

const productLinks = [
  { labelKey: 'image' as const, href: '/image' },
  { labelKey: 'video' as const, href: '/video' },
  { labelKey: 'audio' as const, href: '/audio' },
  { labelKey: 'pricing' as const, href: '/pricing' },
];

const legalLinks = [
  { label: 'Privacy', href: '/privacy' },
  { label: 'Terms', href: '/terms' },
];

export default function Footer() {
  const t = useTranslations('footer');
  const tn = useTranslations('navbar');

  return (
    <footer className="pb-20 md:pb-0" style={{ position: 'relative', background: '#080808', overflow: 'hidden' }}>

      <div style={{ position: 'absolute', top: 0, left: 0, right: 0, height: '1px', background: 'linear-gradient(90deg, transparent 0%, rgba(120,80,255,0.4) 30%, rgba(83,47,207,0.7) 50%, rgba(120,80,255,0.4) 70%, transparent 100%)', pointerEvents: 'none' }} />

      <div aria-hidden="true" style={{ position: 'absolute', bottom: '-20px', left: '50%', transform: 'translateX(-50%)', fontFamily: 'var(--font-clash)', fontWeight: 700, fontSize: 'clamp(80px, 14vw, 160px)', letterSpacing: '-0.04em', whiteSpace: 'nowrap', background: 'linear-gradient(180deg, rgba(255,255,255,0.04) 0%, transparent 100%)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text', pointerEvents: 'none', userSelect: 'none', lineHeight: 1 }}>
        keyo.studio
      </div>

      <div style={{ position: 'absolute', bottom: '-40px', left: '50%', transform: 'translateX(-50%)', width: '600px', height: '300px', background: 'radial-gradient(ellipse at center, rgba(83,47,207,0.18) 0%, transparent 68%)', pointerEvents: 'none' }} />

      <div aria-hidden="true" style={{ position: 'absolute', inset: 0, backgroundImage: 'radial-gradient(rgba(120,80,255,0.07) 1px, transparent 1px)', backgroundSize: '28px 28px', maskImage: 'radial-gradient(ellipse 70% 80% at 50% 100%, black 0%, transparent 80%)', WebkitMaskImage: 'radial-gradient(ellipse 70% 80% at 50% 100%, black 0%, transparent 80%)', pointerEvents: 'none' }} />

      <div className="relative font-dm" style={{ padding: '40px 32px 28px' }}>

        <div className="flex flex-col items-center text-center" style={{ marginBottom: '36px' }}>
          <div className="inline-flex items-center gap-2" style={{ background: 'rgba(83,47,207,0.1)', border: '0.5px solid rgba(83,47,207,0.25)', borderRadius: '20px', padding: '3px 10px', marginBottom: '14px' }}>
            <span style={{ color: 'rgba(120,80,255,0.8)', fontSize: '8px' }}>✦</span>
            <span style={{ color: 'rgba(120,80,255,0.6)', fontSize: '10px', fontWeight: 500, letterSpacing: '0.8px', textTransform: 'uppercase' }}>{t('badge')}</span>
          </div>
          <p className="font-clash" style={{ fontSize: 'clamp(22px, 3.5vw, 38px)', fontWeight: 700, letterSpacing: '-0.03em', lineHeight: 1.2, margin: 0, background: 'linear-gradient(135deg, #e8e0ff 0%, #c4b0ff 50%, #a080ff 100%)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>
            {t('tagline')}
          </p>
        </div>

        <div className="flex flex-col md:flex-row md:items-center justify-between gap-4 items-center md:items-stretch" style={{ marginBottom: '20px' }}>
          <div className="flex justify-center md:justify-start"><Logo size={16} /></div>

          <div className="flex items-center gap-0 justify-center">
            {productLinks.map((link, i) => (
              <React.Fragment key={link.href}>
                <Link href={link.href} className="font-dm transition-colors duration-200 hover:text-white" style={{ fontSize: '13px', color: 'rgba(255,255,255,0.32)', textDecoration: 'none' }}>
                  {tn(link.labelKey)}
                </Link>
                {i < productLinks.length - 1 && <span style={{ color: 'rgba(255,255,255,0.1)', margin: '0 10px', fontSize: '12px' }}>·</span>}
              </React.Fragment>
            ))}
          </div>

          <div className="flex items-center gap-1.5">
            {socials.map((s) => (
              <a key={s.label} href={s.href} target="_blank" rel="noopener noreferrer" aria-label={s.label} className="flex items-center justify-center transition-all duration-200 hover:opacity-80 hover:-translate-y-0.5" style={{ width: 28, height: 28, background: 'rgba(255,255,255,0.04)', border: '0.5px solid rgba(255,255,255,0.07)', borderRadius: '7px', opacity: 0.4 }}>
                <svg width="11" height="11" viewBox="0 0 24 24" fill="white" stroke="none"><path d={s.path} /></svg>
              </a>
            ))}
          </div>
        </div>

        <div style={{ height: '0.5px', background: 'rgba(255,255,255,0.05)', marginBottom: '16px' }} />

        <div className="flex flex-col md:flex-row items-center justify-between gap-2">
          <div className="flex items-center gap-3">
            <p className="font-dm" style={{ fontSize: '11px', color: 'rgba(255,255,255,0.18)', margin: 0 }}>© 2026 keyo.studio</p>
            <div className="inline-flex items-center gap-1" style={{ background: 'rgba(83,47,207,0.08)', border: '0.5px solid rgba(83,47,207,0.18)', borderRadius: '20px', padding: '2px 8px' }}>
              <span style={{ color: 'rgba(120,80,255,0.7)', fontSize: '8px' }}>✦</span>
              <span style={{ color: 'rgba(120,80,255,0.5)', fontSize: '10px', fontWeight: 500 }}>{t('poweredBy')}</span>
            </div>
          </div>
          <div className="flex items-center">
            {legalLinks.map((link, i) => (
              <React.Fragment key={link.label}>
                {i > 0 && <span style={{ color: 'rgba(255,255,255,0.08)', fontSize: '11px', margin: '0 8px' }}>·</span>}
                <Link href={link.href} className="font-dm hover:text-white/40 transition-colors duration-200" style={{ fontSize: '11px', color: 'rgba(255,255,255,0.18)', textDecoration: 'none' }}>
                  {link.label}
                </Link>
              </React.Fragment>
            ))}
          </div>
        </div>

        <div className="flex flex-col items-center text-center" style={{ marginTop: '14px', gap: '3px' }}>
          <p className="font-dm" style={{ fontSize: '10px', color: 'rgba(255,255,255,0.1)', margin: 0, lineHeight: 1.6 }}>
            ФОП БОНДАР ОЛЕНА СЕРГІЇВНА · РНОКПП / ЄДРПОУ: 3403609226
          </p>
          <p className="font-dm" style={{ fontSize: '10px', color: 'rgba(255,255,255,0.1)', margin: 0, lineHeight: 1.6 }}>
            Україна, м. Київ, вул. І. Франка 42, 02000 · +380965674504 ·{' '}
            <a href="mailto:info@ellenkeyo.com" className="hover:text-white/30 transition-colors duration-200" style={{ color: 'inherit', textDecoration: 'none' }}>info@ellenkeyo.com</a>
          </p>
        </div>
      </div>
    </footer>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/layout/Footer.tsx
git commit -m "feat(i18n): translate Footer"
```

---

## Task 12: Update CommunityGallery

**Files:**
- Modify: `components/home/CommunityGallery.tsx`

- [ ] **Step 1: Add `'use client'`, `useTranslations`, and swap hardcoded strings**

```tsx
// components/home/CommunityGallery.tsx
'use client';
import { Link } from '@/i18n/navigation';
import { useTranslations } from 'next-intl';

const galleryVideos = [
  { id: 1, prompt: 'portrait of a young woman, natural window light, Canon 50mm', src: 'https://cdn.higgsfield.ai/job_set_chain_preset/2b5ee036-8873-4871-a3f8-4ed4eeab1721.mp4' },
  { id: 2, prompt: 'Tokyo street at night, rain, neon lights, shallow depth', src: 'https://cdn.higgsfield.ai/job_set_chain_preset/06bf5a41-ed5e-49bf-88c6-2d5a7aa8acdb.mp4' },
  { id: 3, prompt: 'studio portrait, dramatic lighting, 85mm lens', src: 'https://cdn.higgsfield.ai/job_set_chain_preset/d4921e56-a83d-435f-9831-709e4aa1e625.mp4' },
  { id: 4, prompt: 'botanical garden, soft golden light, film grain', src: 'https://cdn.higgsfield.ai/job_set_chain_preset/f3617f5e-64d8-41de-bbf5-3648e8229e49.mp4' },
  { id: 5, prompt: 'candid street portrait, natural light, film look', src: 'https://cdn.higgsfield.ai/job_set_chain_preset/def7ae61-e8cb-4ddc-809b-fd13473995ee.mp4' },
  { id: 6, prompt: 'night city reflections, long exposure, moody', src: 'https://cdn.higgsfield.ai/job_set_chain_preset/d29483b9-aa2b-4c59-9aa7-37044bc2c4c0.mp4' },
];

export default function CommunityGallery() {
  const t = useTranslations('gallery');

  return (
    <section
      className="animate-fadeUp delay-200"
      style={{ background: 'var(--bg-card)', border: 'var(--border)', borderRadius: 'var(--radius-card)', padding: '16px', marginBottom: '30px' }}
    >
      <div className="flex items-center justify-between" style={{ marginBottom: '14px' }}>
        <div>
          <p className="font-[600] text-white" style={{ fontSize: '12px', letterSpacing: '0.08em', textTransform: 'uppercase', margin: 0, fontFamily: 'var(--font-clash)' }}>
            {t('title')}
          </p>
          <p className="font-dm" style={{ fontSize: '11px', color: '#555', margin: '2px 0 0' }}>
            {t('subtitle')}
          </p>
        </div>
        <Link href="/explore" className="font-dm font-[500] hover:text-white transition-colors duration-200" style={{ fontSize: '12px', color: 'var(--accent)' }}>
          {t('seeAll')}
        </Link>
      </div>

      <div className="grid grid-cols-3 md:grid-cols-6 gap-2">
        {galleryVideos.map((item) => (
          <div key={item.id} className="group relative overflow-hidden" style={{ aspectRatio: '3/4', borderRadius: 'var(--radius-btn)', cursor: 'pointer', background: '#0a0a0a' }}>
            <video src={item.src} autoPlay muted loop playsInline className="absolute inset-0 w-full h-full object-cover" />
            <div className="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-200 flex flex-col justify-end" style={{ background: 'linear-gradient(to top, rgba(0,0,0,0.88) 0%, transparent 55%)', borderRadius: 'var(--radius-btn)', padding: '10px 8px 8px' }}>
              <p className="font-dm text-white" style={{ fontSize: '9px', lineHeight: '1.4', opacity: 0.8, marginBottom: '6px', display: '-webkit-box', WebkitLineClamp: 2, WebkitBoxOrient: 'vertical', overflow: 'hidden' }}>
                {item.prompt}
              </p>
              <button className="font-dm font-[600] text-white w-full" style={{ fontSize: '9px', background: 'var(--accent)', border: 'none', borderRadius: '4px', padding: '4px 6px', cursor: 'pointer' }}>
                {t('tryPrompt')}
              </button>
            </div>
          </div>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/home/CommunityGallery.tsx
git commit -m "feat(i18n): translate CommunityGallery"
```

---

## Task 13: Update Home page

**Files:**
- Modify: `app/[locale]/page.tsx`

- [ ] **Step 1: Add `useTranslations` and replace all hardcoded strings**

At the top of the file, add the import and swap text. The only changes are: add `useTranslations`, replace `cycleWords`, and replace every static string. The function body starts with:

```tsx
'use client';

import { useEffect, useRef, useState } from 'react';
import { Link } from '@/i18n/navigation';
import { useTranslations } from 'next-intl';
import Navbar from '@/components/layout/Navbar';
import Footer from '@/components/layout/Footer';
import ToolCard from '@/components/home/ToolCard';
import CommunityGallery from '@/components/home/CommunityGallery';
```

Inside the `Home` component, replace the `cycleWords` constant and add `t`:

```tsx
export default function Home() {
  const t = useTranslations('home');
  const cycleWords = [t('cycleWord0'), t('cycleWord1'), t('cycleWord2')] as const;
  // ... rest of hooks unchanged ...
```

Replace the `SectionHeader` calls:
```tsx
<SectionHeader badge={t('sectionImageBadge')} title={t('sectionImageTitle')} href="/image" linkLabel={t('sectionImageLink')} />
// ...
<SectionHeader badge={t('sectionVideoBadge')} title={t('sectionVideoTitle')} href="/video" linkLabel={t('sectionVideoLink')} />
```

Replace the hero badge:
```tsx
<span className="font-dm" style={{ ... }}>{t('badge')}</span>
```

Replace hero h1 text spans:
```tsx
<span className="font-clash" style={{ ... }}>{t('heroLine1')}</span>
// cycling word block unchanged (uses cycleWords array)
<span className="font-clash" style={{ ... }}>
  {t('heroLine3')}{' '}
  <span style={{ ... }}>keyo.studio</span>
</span>
```

Replace subtitle:
```tsx
<p className="font-dm" style={{ ... }}>{t('subtitle')}</p>
```

Replace "Try prompt" in the image hover overlay:
```tsx
<span className="font-dm text-white" style={{ ... }}>{t('tryPrompt')}</span>
```

- [ ] **Step 2: Commit**

```bash
git add app/\[locale\]/page.tsx
git commit -m "feat(i18n): translate Home page"
```

---

## Task 14: Update Pricing page

**Files:**
- Modify: `app/[locale]/pricing/page.tsx`

- [ ] **Step 1: Add imports and swap strings**

Add at top:
```tsx
import { useTranslations } from 'next-intl';
```

Inside `PricingPage` component:
```tsx
const t = useTranslations('pricing');
```

Replace every hardcoded string:
- `"Simple pricing"` → `{t('badge')}`
- `"Create more,"` → `{t('title')}`
- `"think less"` → `{t('titleAccent')}`
- `"One credit system..."` → `{t('subtitle')}`
- `"Most popular"` → `{t('mostPopular')}`
- `"credits / month"` → `{t('creditsPerMonth')}`
- `"/mo"` → `{t('perMonth')}`
- `"No contracts · Cancel anytime ·"` → `{t('footerNote')}`
- `"Enterprise plan →"` → `{t('enterprisePlan')}`
- `'Loading…'` inside the button → `t('loading')`

- [ ] **Step 2: Commit**

```bash
git add app/\[locale\]/pricing/page.tsx
git commit -m "feat(i18n): translate Pricing page"
```

---

## Task 15: Update Privacy and Terms pages

**Files:**
- Modify: `app/[locale]/privacy/page.tsx`
- Modify: `app/[locale]/terms/page.tsx`

- [ ] **Step 1: Rewrite Privacy page to use translations**

```tsx
// app/[locale]/privacy/page.tsx
import type { Metadata } from 'next';
import { getTranslations } from 'next-intl/server';
import Navbar from '@/components/layout/Navbar';
import Footer from '@/components/layout/Footer';

export async function generateMetadata({ params }: { params: { locale: string } }): Promise<Metadata> {
  const t = await getTranslations({ locale: params.locale, namespace: 'privacy' });
  return {
    title: t('title'),
    description: 'Learn how Keyo Studio collects, uses, and protects your personal data.',
    alternates: { canonical: `/${params.locale}/privacy` },
    robots: { index: true, follow: true },
  };
}

export default async function PrivacyPage({ params }: { params: { locale: string } }) {
  const t = await getTranslations({ locale: params.locale, namespace: 'privacy' });
  const sections = (t.raw('sections') as Array<{ title: string; body: string }>);

  return (
    <>
      <Navbar />
      <main className="pb-20 md:pb-0" style={{ paddingTop: '60px', background: 'var(--bg)', minHeight: '100vh' }}>

        <div style={{ padding: '48px clamp(16px,6vw,80px) 36px', borderBottom: '0.5px solid rgba(255,255,255,0.06)' }}>
          <div style={{ maxWidth: '720px', margin: '0 auto' }}>
            <div className="inline-flex items-center gap-1.5 mb-4" style={{ background: 'rgba(83,47,207,0.1)', border: '0.5px solid rgba(83,47,207,0.25)', borderRadius: '20px', padding: '3px 10px' }}>
              <span style={{ color: 'rgba(120,80,255,0.8)', fontSize: '8px' }}>✦</span>
              <span className="font-dm" style={{ color: 'rgba(120,80,255,0.65)', fontSize: '10px', fontWeight: 600, letterSpacing: '1px', textTransform: 'uppercase' }}>{t('badge')}</span>
            </div>
            <h1 className="font-clash" style={{ fontSize: 'clamp(28px,5vw,48px)', fontWeight: 700, letterSpacing: '-0.03em', color: '#fff', margin: '0 0 10px' }}>
              {t('title')}
            </h1>
            <p className="font-dm" style={{ fontSize: '13px', color: 'rgba(255,255,255,0.3)', margin: 0 }}>
              {t('lastUpdated')}
            </p>
          </div>
        </div>

        <div style={{ padding: '40px clamp(16px,6vw,80px) 80px' }}>
          <div style={{ maxWidth: '720px', margin: '0 auto' }}>
            <p className="font-dm" style={{ fontSize: '14px', color: 'rgba(255,255,255,0.45)', lineHeight: '1.8', marginBottom: '36px' }}>
              {t('intro')}
            </p>
            <div style={{ display: 'flex', flexDirection: 'column', gap: '32px' }}>
              {sections.map((s) => (
                <div key={s.title}>
                  <h2 className="font-clash" style={{ fontSize: '16px', fontWeight: 600, color: 'rgba(255,255,255,0.85)', letterSpacing: '-0.01em', marginBottom: '10px' }}>
                    {s.title}
                  </h2>
                  <p className="font-dm" style={{ fontSize: '13px', color: 'rgba(255,255,255,0.4)', lineHeight: '1.8', margin: 0 }}>
                    {s.body}
                  </p>
                </div>
              ))}
            </div>
          </div>
        </div>

        <Footer />
      </main>
    </>
  );
}
```

- [ ] **Step 2: Rewrite Terms page identically** — same structure as Privacy, but `namespace: 'terms'` and `t('title')` = "Terms of Service".

```tsx
// app/[locale]/terms/page.tsx
import type { Metadata } from 'next';
import { getTranslations } from 'next-intl/server';
import Navbar from '@/components/layout/Navbar';
import Footer from '@/components/layout/Footer';

export async function generateMetadata({ params }: { params: { locale: string } }): Promise<Metadata> {
  const t = await getTranslations({ locale: params.locale, namespace: 'terms' });
  return {
    title: t('title'),
    description: 'Read the Terms of Service for Keyo Studio.',
    alternates: { canonical: `/${params.locale}/terms` },
    robots: { index: true, follow: true },
  };
}

export default async function TermsPage({ params }: { params: { locale: string } }) {
  const t = await getTranslations({ locale: params.locale, namespace: 'terms' });
  const sections = (t.raw('sections') as Array<{ title: string; body: string }>);

  return (
    <>
      <Navbar />
      <main className="pb-20 md:pb-0" style={{ paddingTop: '60px', background: 'var(--bg)', minHeight: '100vh' }}>

        <div style={{ padding: '48px clamp(16px,6vw,80px) 36px', borderBottom: '0.5px solid rgba(255,255,255,0.06)' }}>
          <div style={{ maxWidth: '720px', margin: '0 auto' }}>
            <div className="inline-flex items-center gap-1.5 mb-4" style={{ background: 'rgba(83,47,207,0.1)', border: '0.5px solid rgba(83,47,207,0.25)', borderRadius: '20px', padding: '3px 10px' }}>
              <span style={{ color: 'rgba(120,80,255,0.8)', fontSize: '8px' }}>✦</span>
              <span className="font-dm" style={{ color: 'rgba(120,80,255,0.65)', fontSize: '10px', fontWeight: 600, letterSpacing: '1px', textTransform: 'uppercase' }}>{t('badge')}</span>
            </div>
            <h1 className="font-clash" style={{ fontSize: 'clamp(28px,5vw,48px)', fontWeight: 700, letterSpacing: '-0.03em', color: '#fff', margin: '0 0 10px' }}>
              {t('title')}
            </h1>
            <p className="font-dm" style={{ fontSize: '13px', color: 'rgba(255,255,255,0.3)', margin: 0 }}>
              {t('lastUpdated')}
            </p>
          </div>
        </div>

        <div style={{ padding: '40px clamp(16px,6vw,80px) 80px' }}>
          <div style={{ maxWidth: '720px', margin: '0 auto' }}>
            <p className="font-dm" style={{ fontSize: '14px', color: 'rgba(255,255,255,0.45)', lineHeight: '1.8', marginBottom: '36px' }}>
              {t('intro')}
            </p>
            <div style={{ display: 'flex', flexDirection: 'column', gap: '32px' }}>
              {sections.map((s) => (
                <div key={s.title}>
                  <h2 className="font-clash" style={{ fontSize: '16px', fontWeight: 600, color: 'rgba(255,255,255,0.85)', letterSpacing: '-0.01em', marginBottom: '10px' }}>
                    {s.title}
                  </h2>
                  <p className="font-dm" style={{ fontSize: '13px', color: 'rgba(255,255,255,0.4)', lineHeight: '1.8', margin: 0 }}>
                    {s.body}
                  </p>
                </div>
              ))}
            </div>
          </div>
        </div>

        <Footer />
      </main>
    </>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add app/\[locale\]/privacy/page.tsx app/\[locale\]/terms/page.tsx
git commit -m "feat(i18n): translate Privacy and Terms pages"
```

---

## Task 16: Update sitemap.ts

**Files:**
- Modify: `app/sitemap.ts`

- [ ] **Step 1: Add locale variants**

```ts
// app/sitemap.ts
import type { MetadataRoute } from 'next';

const baseUrl = 'https://keyo.studio';
const locales = ['en', 'ua'] as const;

const routes = ['', '/image', '/video', '/audio', '/pricing', '/privacy', '/terms'];

export default function sitemap(): MetadataRoute.Sitemap {
  return locales.flatMap((locale) =>
    routes.map((route) => ({
      url: `${baseUrl}/${locale}${route}`,
      lastModified: new Date(),
      changeFrequency: route === '' ? 'weekly' : 'monthly' as const,
      priority: route === '' ? 1 : 0.8,
    }))
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add app/sitemap.ts
git commit -m "feat(i18n): update sitemap with locale variants"
```

---

## Task 17: Final verification

- [ ] **Step 1: Run build**

```bash
npm run build 2>&1 | tail -50
```

Expected: exit 0, no TypeScript errors, no missing module errors.

- [ ] **Step 2: Start dev server and verify EN locale**

```bash
npm run dev
```

Open `http://localhost:3000` — should redirect to `http://localhost:3000/en` (or `/ua` if system is Ukrainian). Verify Navbar shows "Explore", "Image", "Video", "Pricing", "EN|UA" pill.

- [ ] **Step 3: Verify UA locale**

Navigate to `http://localhost:3000/ua` — Navbar should show "Огляд", "Зображення", "Відео", "Ціни". Footer tagline should be "Створюй будь-що за допомогою AI".

- [ ] **Step 4: Verify locale switcher**

Click "UA" in the EN|UA pill while on `/en/pricing` — should navigate to `/ua/pricing` with Ukrainian strings.

- [ ] **Step 5: Verify Privacy and Terms in both locales**

- `http://localhost:3000/en/privacy` — English title "Privacy Policy"
- `http://localhost:3000/ua/privacy` — Ukrainian title "Політика конфіденційності"

- [ ] **Step 6: Final commit if any fixes were needed**

```bash
git add -A
git commit -m "fix(i18n): address build/runtime issues found in QA"
```
